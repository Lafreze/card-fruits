# 《叠个果王》设计文档(存档)

> 一句话玩法:从叠放的水果卡片中挑出相同水果,三张相同消除并生成实体水果,再通过物理碰撞不断合成,最终合出"果王"。
> 宣传语:点三个,掉一个,碰两个,合成果王!

融合:《羊了个羊》叠层选择+卡槽压力 / 《合成大西瓜》物理碰撞 / 《2048》升级成长 / 《挑一挑》寻找解锁感。

## 三个区域

1. **叠层区**(上):互相遮挡的水果卡片,只有未被压住的可点。6 种卡片:葡萄/樱桃/草莓/橙子/苹果/桃子。
2. **卡槽区**(中):7 格。相同卡片自动归组;凑满 3 张→爆裂消除→生成实体水果落入合成箱。7 张无法消除即失败。
3. **合成箱**(下):透明物理箱。左右滑动选择掉落位置,两个相同水果碰撞→合成高一级。

## 合成路线(9 级)

葡萄→樱桃→草莓→橙子→苹果→桃子→哈密瓜→西瓜→黄金果王

## 胜负

- 胜利:合成关卡目标水果(每日挑战:清空全部卡片+合成黄金果王)。
- 失败:①卡槽 7 张满且无法三消;②合成箱水果超警戒线持续 2 秒(超线先进入红色警报,给自救机会)。

## 分数

- 三消:水果等级 × 30
- 合成:50 × 2^(等级-1)(樱桃100 … 黄金果王12800)
- 连击(2 秒内连续三消/合成):2连×1.25 / 3连×1.5 / 4连×2 / 5连×2.5 / 6连+×3
- 连锁演出:2连蓝色冲击波 / 3连紫色拖尾 / 4连金色慢动作 / 5连+全屏彩虹;文案:太甜了!/果汁爆发!/超级连锁!/果王降临!

## 道具(首版 4 个)

撤回(每局免费1次)/ 洗牌(不改遮挡层级)/ 榨汁机(水果降一级)/ 万能果(万能三消)。

## 特殊卡片(后续)

冰冻卡、锁链卡、问号卡、传送带、石头水果(只能被高级合成冲击波破坏)、果汁炸弹。

## 关卡难度参数

卡片种类 3→6 / 叠层深度 2→6 / 初始可点数量 / 合成箱宽度 / 目标水果等级 / 特殊卡数量。
生成规则:每种普通卡数量为 3 的倍数;至少一条通关路径(自动求解器验证);前五次点击至少可组一组三消;至少两种可行解法。

## 章节

新手 4 关(点击→合成→卡槽压力→警戒线)→ 清晨果摊(1-20)→ 夏日果园(21-50)→ 水果夜市(51-100)。

## 商业化 / 留存(摘要)

激励广告仅玩家主动触发(复活/清顶/撤回/翻倍),每局最多复活一次;内购只卖外观与去广告。图鉴多外观、签到、赛季换皮(甜品季/海洋季/星球季…)。每日挑战固定种子+操作回放防作弊,战绩卡分享("我只差一个草莓就能合出果王,你能过吗?")。

## 三大爽点

1. 卡槽将满时最后一张卡触发三消,死里逃生。
2. 一次掉落触发连锁合成,连环爆炸(短视频传播点)。
3. 最后一张卡→三消→物理连锁→黄金果王诞生的完整高潮。

## 首版 MVP

1 主题 / 9 级水果 / 60 主线关 / 每日挑战 / 4 特殊卡 / 4 道具 / 排行榜 / 图鉴 / 复活 / 分享。
技术:卡片记录 id/fruitType/position/layer/coveredBy/state;物理引擎(Matter.js 等);合成时先锁定两果再生成防重复;每日挑战固定随机种子。

---

## 当前原型实现状态(2026-07-16)

早期 `index.html` / `game.html` 原型使用自研 Canvas + 圆形刚体物理，已归档到 `legacy/game.html`。

第二轮完善(修复"目标高于卡片种类时玩家不知道要碰撞合成"的卡关问题):

- 开场横幅教合成路径("目标 🍊:用 🍓+🍓 碰撞合成!"),箱底常驻 9 级合成路线图并高亮目标;
- 同级水果磁吸:安定后互相缓慢靠拢,残局同级水果必然相遇,消除"水果散在箱底碰不到"的死局;
- 三消直接产出目标水果也判胜;
- 残局自动判负:无牌可点且剩余水果两两合并推演到顶仍达不到目标 → "剩下的水果不够合成目标了";
- 榨汁机道具(×1):点选箱内水果降一级,疏通堵局;
- 三消/合成得分浮字;第二关 [6,6,6]→[6,6,9] 增加容错。

第三轮(2026-07-16):特殊卡片 + 特效 + UI + 水果扩充:

- 水果 9→11 级:新增 柠檬🍋(草莓后)、菠萝🍍(桃子后);卡片种类最多 7 种;关卡 10→11 关,全部按"两两合并推演"验算可达性并留 ≥25% 容错;
- 特殊卡片:❄冰冻(拾走旁边的卡解冻)/ ⛓锁链(任意三消震断)/ ❓问号(点击才揭晓),第 4/6/8 关起分别引入;全部可点卡被冰/链锁死时 1.6s 自动救济降一层;
- 合成特效:emoji 果肉碎片旋转飞溅、星光四芒闪、4 连金闪+慢动作、西瓜/果王触发"大果合成!"全屏暗场聚焦+三重冲击波+专属低音;果王额外皇冠雨;
- 磁吸补强:同级水果被其他水果夹住拉不动时,小的会起跳翻越障碍(起跳力度按尺寸计算),解决"樱桃-橙子-樱桃"三明治僵局;
- UI:遮挡越深卡片越暗、横幅底板胶囊+特殊卡教学副行、得分跳动动效、警戒线倒计时警示文字、过关三星评价(3连锁/无道具)、主菜单玩法图示+全水果链。

第四轮(2026-07-16):无尽模式 + 水果扩充:

- 水果 11→13 级:新增 猕猴桃🥝(柠檬后)、芒果🥭(桃子后);主线 11→12 关全部重新验算(容错 33%~59%);
- 终章(第 12 关)引入高阶卡片(桃子/芒果/菠萝/哈密瓜/西瓜卡),两组西瓜三消 → 双西瓜碰撞 → 果王,解决果王 4096 单位价值靠低阶卡不可达的问题;
- **无尽模式**(通关全部 12 关解锁,levelIdx=-1):初始 4 种卡 24 张,每次三消自动补一组 3 张同类新卡(种类池每 8 组扩一种、上限 9 种;特殊卡概率随进度升至 30%;卡上限 57 张时暂停补牌);每 10 组三消奖励随机道具;无目标不判残局,死亡即结算(得分/组数/最大水果),独立最高分 gw.endlessBest;
- 双果王碰撞 → 彩虹爆发:清空全场水果 +100000×连击分(无尽终极爽点,主线果王即胜利所以基本只在无尽出现);
- 主菜单通关后出现 ∞ 无尽模式按钮与无尽最高分;未通关显示解锁提示;终章胜利页直通无尽;
- 无尽路线图高亮"最大"已达成水果(代替"目标")。

未含:传送带/石头/炸弹特殊卡、万能果、每日挑战、图鉴、商业化。

---

## Web Game 2.0（2026-07-16）

- 工程升级为 React 19 + TypeScript + Vite，游戏画面迁移到 PixiJS 8，物理世界迁移到 Matter.js；
- 12 关主线、叠层解锁、七格卡槽、三消掉落、物理合成、警戒线、撤回/洗牌/榨汁完整保留；
- 新增霓虹果园视觉、动态星空、卡片光效、水果光晕、粒子爆裂、冲击波、震屏、连击浮字、手游式结算与触觉反馈；
- 新增 Express + PostgreSQL 服务端，游戏结束后输入用户名保存分数，支持全球排行榜与单局防重复提交；
- 新增 Railway/Nixpacks 部署配置、健康检查、安全响应头、提交限流与生产静态托管。

### 玩法修正（2026-07-16，第二次）

重构时丢失/写错的旧版核心机制已找回：

- 磁吸修复：原公式越近吸力越小且不足以克服 Matter 地面静摩擦（约 108 px/s²），残局同级水果碰不到一起；改回旧版"越近越强"曲线（150~430 px/s²，速度增量实现）；
- 起跳翻越找回：同级水果被夹住拉不动 1.1s 后，朝对方起跳翻越障碍（不限距离），杜绝"三明治"软锁；
- 残局判负找回"两两合并推演"（`canReachTarget`），并排除顶级水果不可合并的误判；
- 玩法计时全部改为游戏时间 `elapsed`（水果年龄/警戒线/残局/连击窗口）：此前用墙钟 `performance.now()`，标签页切后台 rAF 暂停但计时照走，回来即被警戒线误判负；
- 胜利保护 `winPending`：合成出目标后的 850ms 庆祝延迟内不再被警戒线/卡槽满抢先判负；
- 卡槽相同卡片自动归组（设计文档既有规则）；警戒线判负时长对齐设计的 2 秒；
- 修复重开/下一关在同一 canvas 上重建 PixiJS 导致 WebGL 上下文丢失卡死（canvas 按局 `key` 重建）、返回主菜单双重 destroy 崩溃、`merging` 集合泄漏；
- 开发模式暴露 `window.__game`，自动化测试可 `for(...) __game.tick({deltaMS:16.67})` 快进逻辑（后台标签页 rAF 不跑，勿靠实时等待）。

### 关卡造型与适配完善（2026-07-16）

- 12 关分别使用缓坡、砖墙、金字塔、双塔、菱形、环形深井、十字祭坛、瀑布和王冠等独立叠层造型；
- 关卡生成会把同类三张放入初始可点区域，避免随机牌序导致开局无法三消；
- 新增关卡数据自动校验：检查卡片/卡位数量、边界、初始可点数、三张倍数和目标合成资源；
- 手机端改为完整等比缩放，窄屏和短屏不再裁掉物理果箱或底部水果。

---

## Web Game 3.0：果园远征与无尽狂欢

### 三种模式定位

1. **主线果园**：12 个手工叠层关卡，负责教学、解锁水果和建立合成目标。每关必须通过至少一次物理碰撞才能获得目标水果。
2. **无尽狂欢**：没有关卡目标，清空卡组后立即进入下一波。波次越高，卡片数量、叠层深度和特殊牌概率越高；每 2 波奖励清顶锤，每 3 波奖励洗牌与磁核。双黄金果王相撞会清空果箱并奖励 `100000 × 得分倍率`。
3. **果园远征（Roguelike）**：连续挑战逐级关卡，分数跨路线继承。每次胜利从 3 件随机奇物中选择 1 件，奇物不重复出现；失败后本次构筑结算，下一次从零开始。

### 单局节奏

- **观察 3–8 秒**：辨认初始可点三消、炸弹位置和冰冻解锁方向。
- **卡槽决策 20–60 秒**：相同卡自动归组；优先完成三消，或利用炸弹连取改变顶部结构。
- **投放决策 2–4 秒**：三消水果悬停在果箱顶部，玩家可点选落点；未操作时自动对准同级水果投放。
- **物理连锁 1–6 秒**：同级水果磁吸、碰撞、合成，连续反应进入倍率窗口。
- **资源决策**：卡槽接近满时使用清顶锤；果箱拥堵时使用榨汁；同级水果被隔开时使用磁核。

### 特殊牌规则

| 特殊牌 | 操作 | 收益与风险 | 克制方式 |
|---|---|---|---|
| ❄ 冰冻 | 默认不可选 | 增加解锁顺序压力 | 收取 95px 内邻卡；全部亮牌冻结时自动救济两张 |
| 💣 炸弹 | 点击即引爆并正常进入卡槽 | 连带收取附近 2 张卡，可能制造三消，也可能快速塞满卡槽 | 先观察卡槽种类；爆浆奇物将范围提高并多收 1 张 |
| 🌿 藤蔓 | 第一次剪藤、第二次收取 | 消耗一次额外点击，打断盲点节奏 | 清顶锤可直接解除并收取 |

### 道具体系

- **撤回**：仅撤回尚未形成三消的最后一张普通卡，保留策略纠错空间。
- **洗牌**：重排剩余卡片的位置与层级，不改变数量和水果资源。
- **榨汁**：将果箱中最高级水果降一级，主动释放空间并改变合成链。
- **清顶锤**：自动选择卡槽最需要的可见牌；同时解除冰冻或藤蔓。
- **合并磁核**：选择距离最近的一对同级水果，立即触发一次物理合成。

### Roguelike 奇物池

| 奇物 | 构筑方向 | 效果 |
|---|---|---|
| 🫧 迷你果园 | 容量流 | 物理水果再缩小 14% |
| ✨ 黄金触感 | 分数流 | 三消、合成得分 +35% |
| 🧺 深口果篮 | 卡槽流 | 卡槽 +1 格 |
| ⏳ 慢糖结界 | 生存流 | 警戒线容错 2.0s → 3.2s |
| 🎒 园丁腰包 | 道具流 | 每关洗牌、清顶锤各 +1 |
| 🧲 引力果核 | 连锁流 | 同级水果磁吸 +55% |
| 💥 爆浆配方 | 炸弹流 | 炸弹率、范围和连取数提高 |
| 🌸 幸运花期 | 开局流 | 每条路线额外生成一颗低阶水果 |

### 空间与视觉平衡

- 13 级水果半径由 `13–78` 调整为 `10–60`，高阶水果仍有体量升级感，但不会两三颗就堵满 396px 宽果箱。
- Roguelike 的迷你果园会在新尺寸上再缩小 14%，形成明确的容量流派优势。
- 单颗水果不再使用独立模糊滤镜，改用双层半透明光环；粒子总量限制为 190，低端手机上连锁爆发不会无限堆积。
- 三消震屏、普通合成震屏和高阶合成震屏分级，保证反馈明显但不影响投放判断。

### 防软锁与公平性

- 每个关卡开局保证 3 张同类牌处于可点区域；卡片总量均为 3 的倍数。
- 主线/远征残局使用“两两合并推演”确认是否仍可达到目标；无尽模式在卡组清空但卡槽残留时结算失败。
- 待投放水果、合并中的水果和胜利演出期间不会提前触发残局判负。
- 炸弹连取仍进入卡槽并遵守七格/八格上限，不提供无成本清牌。
- 无尽、远征和主线分别记录排行榜，避免不同计分结构互相污染。

### 后续平衡指标

- 主线 1–3 关目标通关率 70%–85%，4–8 关 45%–65%，9–12 关 25%–45%。
- 无尽模式中位到达波次目标为 4，高手目标为 9+；双果王清场应低于 3% 对局。
- 远征首次选择奇物后，平均继续路线数目标提升 1.5；单件奇物选择率长期不应低于 8% 或高于 22%。
- 卡槽失败与警戒线失败比例目标约 60:40；若单侧超过 75%，优先调整特殊牌率或水果尺寸。
